#include <stdio.h>

int comp(int A, int B, int cardA[A], int cardB[B]);

int main(void) {
  while(1){
    int A, B, j, cardA[A], cardB[B];
      scanf("%d %d", &A, &B);
      if (A == 0 && B == 0 &&){
        break;
      }
      for(j = 0; j < A; j++){
        scanf("%d", &cardA[i]);
      }
      for(j = 0; j < B; j++){
        scanf("%d", &cardB[i]);
      }
      comp(A, B, A, cardB);
  }
  return 0;
}

int comp(int A, int B){
  int i, k, maior = A;
  if (A < B){
    val = B
  }
  for(i = 0; i < val; i++){
      
  }
}
