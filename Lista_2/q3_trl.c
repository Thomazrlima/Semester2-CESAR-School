#include <stdio.h>

int comp(int Al, int Be, int A[Al], int B[Be]);

int main(void) {
  while(1){
    int Al, Be, j, A[Al], B[Be];
      scanf("%d %d", &Al, &Be);
      if (Al == 0 && Be == 0 &&){
        break;
      }
      for(j = 0; j < Al; j++){
        scanf("%d", &A[i]);
      }
      for(j = 0; j < Al; j++){
        scanf("%d", &A[i]);
      }
      comp(Al, Be, A, B);
  }
  return 0;
}

int comp(int Al, int Be){
  int i, k, maior = Al;
  if (Al < Be){
    val = Be
  }
  for(i = 0; i < val; i++){
      
  }
}
